# Eniac Execution: WUT v1.0.0 Cross-Platform Release

> Owner: wut-v1-release
> Status: active

## Contract

- **Goal:** Prepare and release WUT version 1.0.0 with cross-platform GitHub Actions builds.
- **Done when:**
  - All hardcoded version strings updated to 1.0.0.
  - Existing CI/CD workflows verified to support v1.0.0 cross-platform builds.
  - Git tag `v1.0.0` pushed and GitHub release workflow triggered.
  - `go test ./...` and `go build` pass.
- **Scope:** Version strings in source, scripts, docs; existing `.github/workflows/*.yml`; git tag push.
- **Preserve:** Existing workflow structure and release behavior; unrelated source changes.
- **Exclude:** Rewriting CI from scratch unless existing workflows are insufficient; creating new installer formats.

## Milestones

- [x] **M1 - Update version to 1.0.0**
  - Proof: only `.eniac-plan.md` still mentions "0.3.0" intentionally; all source/docs/scripts are 1.0.0.
- [x] **M2 - Verify CI workflows**
  - Proof: `release.yml` builds linux/macos/windows-x64 and publishes release on tag push; `go test ./...` passes; `wut --version` reports `1.0.0`.
- [ ] **M3 - Push tag and trigger release**
  - Proof: `git tag v1.0.0` and `git push origin v1.0.0` executed with user confirmation; GitHub Actions release run observed.

## Active State

- **Current:** M3 - waiting for user authorization to push tag.
- **Invariant:** `go test ./...` must pass before any tag is pushed.
- **Decision:** Keep existing release workflow; only update version constants.
- **Last signal:** `go test ./...` passes; `wut --version` reports 1.0.0.
- **Next:** Ask user to authorize `git tag v1.0.0` and `git push origin v1.0.0`.
- **Verify:** GitHub Actions release workflow runs and publishes assets.
- **Blocker:** awaiting explicit authorization for git tag/push (external publication).
- **Risk:** Pushing tag v1.0.0 is irreversible and triggers public release.
